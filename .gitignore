# Node.js dependencies
# This is the most important line. The node_modules folder can be huge
# and should never be committed to version control. It can be easily
# recreated by running 'npm install'.
/node_modules

# Log files
# Keep any log files generated by your application or tools out of the repo.
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables file
# In the future, you might add a .env file to store secrets like database
# passwords or API keys. This ensures you never accidentally commit them.
.env

# OS-generated files
# These are junk files created by macOS and Windows.
.DS_Store
Thumbs.db

# VS Code editor configuration
# Keep your personal editor settings out of the project repository.
.vscode/